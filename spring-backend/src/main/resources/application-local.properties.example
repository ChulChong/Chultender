# Copy this to application-local.properties (gitignored) and fill in the
# real value from Supabase: Project Settings > Database > Connection string.
spring.datasource.password=

# Password for the hidden /Admin entry point (Chultender.js's brand-dot
# click handler). Pick your own value.
admin.password=
